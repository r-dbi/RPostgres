% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signal_pq_error.R
\name{RPostgres-conditions}
\alias{RPostgres-conditions}
\title{RPostgres error conditions}
\description{
When a server-side error occurs, RPostgres raises a classed \code{\link[rlang:abort]{rlang::abort()}}
condition that carries PostgreSQL's \code{SQLSTATE} code and the diagnostic fields
reported by \code{libpq}. The class vector of the condition is
\code{c("RPostgres_error_<SQLSTATE>", "RPostgres_error", "rlang_error", "error", "condition")}, so handlers can dispatch on a specific code or on all RPostgres
errors.

The following fields are attached to the condition object (any of them may be
\code{NULL} if \code{libpq} did not supply a value): \code{sqlstate}, \code{hint}, \code{detail},
\code{context}, \code{table}, \code{column}, \code{datatype}, \code{constraint}, and \code{schema}. They map
to the corresponding \verb{PG_DIAG_*} fields reported by libpq's
\code{PQresultErrorField()}.

Errors that are not associated with a server result (for example a lost
connection) still carry the \code{RPostgres_error} class but have no \code{sqlstate}.
}
\examples{
\dontrun{
# Dispatch on a specific SQLSTATE
tryCatch(
  dbGetQuery(con, "SELECT * FROM no_such_table"),
  RPostgres_error_42P01 = function(e) e$sqlstate
)

# Read the SQLSTATE programmatically
cnd <- tryCatch(
  dbGetQuery(con, "SELECT * FROM no_such_table"),
  RPostgres_error = identity
)
cnd$sqlstate
}
}
